<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kot.model.dao.ReplyDao">

	<select id="allReply" resultType="Reply">
		SELECT * FROM reply
		WHERE boardId = #{boardId}
	</select>

	<insert id="allReply" parameterType="Reply">
		INSERT INTO reply
		VALUES (0, #{boardId}, #{userId}, #{content}, now(), false)
	</insert>
	
	<update id="updateReply" parameterType="Reply">
		UPDATE reply
		SET content = #{content}
		WHERE replyId = #{replyId}
	</update>

	<update id="deleteReply" parameterType="Reply">
		UPDATE reply
		SET delstatus = true
		WHERE replyId = #{replyId}
	</update>

</mapper>