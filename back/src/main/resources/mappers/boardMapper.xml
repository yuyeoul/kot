<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kot.model.dao.BoardDao">

	<insert id="writeBoard" parameterType="Board">
		INSERT INTO board
		VALUES (0, #{userId}, #{title}, #{content}, #{regdate}, 0, false)
	</insert>
	
	<update id="updateBoard" parameterType="Board">
		UPDATE board
		SET title = #{title},content=#{content}
		WHERE boardId = #{boardId}
	</update>
	
	<update id="deleteBoard" parameterType="Board">
		UPDATE board
		SET delstatus=true
		WHERE boardId = #{boardId}
	</update>
	
	<select id="totalBoard" resultType="Board">
		SELECT *
		FROM board
		where delstatus=false
	</select>
	
	<select id="oneBoard" resultType="Board">
		SELECT *
		FROM board
		where boardId=#{boardId}
	</select>	
	
	<select id="searchBoard" parameterType="String" resultType="Board">
		SELECT *
		FROM board
		WHERE title LIKE concat('%', #{search}, '%') or content LIKE concat('%', #{search}, '%')
	</select>

</mapper>